# Ignore everything
*

# But not these directories (so rules inside them are processed)
!*/

# Whitelist .c and .h files anywhere
!*.c
!*.h
!*.md
!.gitignore
!*.supp
# Whitelist Makefiles anywhere
!Makefile

